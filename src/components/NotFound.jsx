export default function NotFound({ text }) {
    return (
        <div className="w-full bg-dark py-[2vw] flex items-center justify-center">
			<p className="text-silver text-center">{ text }</p>
		</div>
    );
}